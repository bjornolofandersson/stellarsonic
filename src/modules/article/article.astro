---
import ArticleTheme from './article.svelte';
import { PageProps } from '@lib/interfaces';
import type { Article } from './article.server';

interface Props extends PageProps<Article> {}

const { entity, site, edit } = Astro.props;
---

{ edit ?
  <ArticleTheme entity={entity} site={site} client:load>
    <slot/>
  </ArticleTheme>
:
  <ArticleTheme entity={entity} site={site}>
    <slot/>
  </ArticleTheme>
}
