---
import { Entity } from '@lib/interfaces';
import Hero from './hero.svelte';

interface Props {
  data: any;

  page: Entity<any>;

  navigation: any;
}

const { data, page, navigation } = Astro.props;
---

<Hero data={data} page={page} navigation={navigation} client:load />
