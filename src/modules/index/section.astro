---
import { Entity } from '@lib/interfaces';
import Hero from './hero.astro';

interface Props {
  data: any;

  page: Entity<any>;

  navigation: any;
}

const { data, page, navigation } = Astro.props;
---

{data.type === 'hero' &&
  <Hero data={data} page={page} navigation={navigation} />
}
