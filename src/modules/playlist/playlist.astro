---
import Playlist from './playlist.svelte';
import MenuBar from '@theme/MenuBar.svelte';
import { MusicMixPost, PageProps } from '@lib/interfaces';

interface Props extends PageProps<MusicMixPost> {}

const { entity, page, navigation } = Astro.props;
---

<Playlist entity={entity} page={page} navigation={navigation} client:load>
  <!--<MenuBar sitemap={navigation} slot="menu" />-->
  <Fragment slot="article">
    <slot/>
  </Fragment>
</Playlist>
